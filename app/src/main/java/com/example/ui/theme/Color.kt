package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Frosted Glass Palette
val ObsidianBg = Color(0xFF0A0A0C)          // Deep slate-black background
val GlassBgSoft = Color(0x0DFFFFFF)         // White with 5% opacity (bg-white/5)
val GlassBgMedium = Color(0x1AFFFFFF)       // White with 10% opacity (bg-white/10)
val GlassBgActive = Color(0x33FFFFFF)       // White with 20% opacity (bg-white/20)

// Vibrant Glow Accents
val IndigoAccent = Color(0xFF6366F1)        // Indigo 500
val IndigoDeep = Color(0xFF4F46E5)          // Indigo 600
val VioletAccent = Color(0xFF8B5CF6)        // Violet 500
val VioletDeep = Color(0xFF7C3AED)          // Violet 600

// Helper accents
val HotPink = Color(0xFFFF0054)             // Alert / High Priority indicator
val BrightYellow = Color(0xFFFBBF24)        // Warning / Medium Priority indicator

// Text & Borders
val SpaceWhite = Color(0xFFF8FAFC)          // Slate-50 / Clean White
val SlateTextMuted = Color(0xFF94A3B8)      // Slate-400 / Subdued text
val GlassBorderSoft = Color(0x1AFFFFFF)     // Translucent border (white/10)
val GlassBorderMedium = Color(0x33FFFFFF)   // Translucent border (white/20)
val IndigoBorderGlow = Color(0x4D6366F1)    // Indigo with 30% opacity
